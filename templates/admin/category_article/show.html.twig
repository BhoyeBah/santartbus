{% extends 'base.html.twig' %}

{% block title %}CategoryArticle{% endblock %}

{% block body %}
    <h1>CategoryArticle</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ category_article.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ category_article.name }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ category_article.createdAt ? category_article.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_category_article_index') }}">back to list</a>

    <a href="{{ path('app_category_article_edit', {'id': category_article.id}) }}">edit</a>

    {{ include('category_article/_delete_form.html.twig') }}
{% endblock %}
